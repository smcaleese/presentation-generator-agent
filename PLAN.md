## Plan
- First create the UI skeleton. This will involve implementing a static chat UI on the left side and a static PDF view on the right side.
- Set up the chatbot: I will then set up DeepSeek V4 Flash and ensure that I can start chatting with it.
- Set up daytona: The next step is to set up Daytona for running the code.
- What should my tech stack be? I think I should set up a database and local Docker stack. A suggested teck stack:
    - React and vite on the frontend
    - Node.js on the backend
    - Some kind of simple NoSQL database like MongoDB
    - All run locally with docker